# constructor is a function which is called automatically when the object is created of a class like

class Car:
    def __init__(self):
        print("this is the constructor")

obj = Car() # here when we run that file it automatically calls up the __init__ function that is the constructorgir